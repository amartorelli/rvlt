apiVersion: v1
kind: Service
metadata:
  name: {{ include "rvlt.fullname" . }}
  labels:
    app: {{ include "rvlt.name" . }}
    chart: {{ include "rvlt.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "rvlt.name" . }}
    release: {{ .Release.Name }}
